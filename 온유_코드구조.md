---
tags: [온유시스템, 코드구조, obsidian_agent]
날짜: 2026-03-29
자동생성: 2026-03-29 18:19
---

# 온유 시스템 코드 구조 (obsidian_agent.py)

> 자동 생성됨 by `onew_code_indexer.py` — 2026-03-29 18:19
> 파일 크기: 277KB | 총 5782줄 | 클래스 5개 | 함수 84개
> **.py 파일은 임베딩 대상 아님** — 이 .md 파일이 RAG 검색 진입점
> 코드 직접 조회: `read_file("SYSTEM/obsidian_agent.py")`
> 인덱스 갱신: `refresh_code_index()` 또는 Claude Code 자동 실행

---

## 1. 핵심 상수 (설정)

| 상수 | 값 / 설명 |
|------|-----------|
| _LOG_DIR | `os.path.join(os.path.dirname(__file__), "logs")` |
| OBSIDIAN_VAULT_PATH | `r"C:\Users\User\Documents\Obsidian Vault"` |
| _PID_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "onew.pid")` |
| DB_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "onew_pure_db.json")` |
| LANCE_DB_DIR | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", ".onew_lance_db")` |
| EMBED_DIM | `3072` |
| SYSTEM_PROMPT_PATH | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "onew_system_prompt.md")` |
| ANTIPATTERNS_PATH | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "onew_antipatterns.md")` |
| USER_PROFILE_PATH | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "User_Profile.md")` |
| MEMORY_DIR | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "memory")` |
| USER_CORE_PATH | `os.path.join(MEMORY_DIR, "core.md")` |
| USER_CONTEXT_PATH | `os.path.join(MEMORY_DIR, "context.md")` |
| USER_ENTITIES_PATH | `os.path.join(MEMORY_DIR, "entities.md")` |
| QUERY_LOG_FILE | `os.path.join(MEMORY_DIR, "query_log.jsonl")` |
| SKILLS_DIR | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "skills")` |
| WORKING_MEMORY_DIR | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "working_memory")` |
| USAGE_LOG_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "api_usage_log.json")` |
| HASH_CACHE_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "onew_content_hashes.json")` |
| SYNC_EXCLUDE_DIRS | `["대화기록", "venv", ".obsidian", ".git",                        "code_backup", ".db` |
| SYNC_EXCLUDE_FILES | `["onew_pure_db", "api_usage_log", "onew_content_hashes"]` |
| MAX_FILE_SIZE_KB | `200` |
| MAX_CHUNKS_PER_FILE | `150` |
| MAX_DAILY_EMBED_CALLS | `10000` |
| MAX_DAILY_CHAT_WARN | `500` |
| AUTO_SYNC_HOURS | `12` |
| CHUNK_VERSION | `"v3"` |
| CORRECTION_KEYWORDS | `["틀렸어", "아니야", "아니잖아", "다시 해줘", "잘못됐어",                         "틀린 것 같아", "그게 아` |
| MISTAKE_LOG_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "오답_패턴.md")` |
| GROWTH_DIR | `os.path.join(OBSIDIAN_VAULT_PATH, "온유_성장기록")` |
| FAILURE_DIR | `os.path.join(GROWTH_DIR, "실패사례")` |
| CODE_LESSON_DIR | `os.path.join(GROWTH_DIR, "코드교훈")` |
| SELF_EVAL_MIN_LEN | `20` |
| SELF_EVAL_MIN_SCORE | `3` |
| IMPORTANCE_RECALC_DAYS | `7` |
| HIT_WINDOW_DAYS | `30` |
| MAX_DAILY_SEARCHES | `30` |
| EXAM_DATE | `datetime(2026, 4, 18)` |
| SEARCH_ALLOW_TOPICS | `[]` |
| PROTECTED_FILES | `{     'obsidian_agent.py',   # 온유 메인 에이전트     'onew_shared.py',      # 공유 상태 모듈 ` |
| CODE_REVIEW_DIR | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "코드리뷰")` |
| PROTECTED_FOLDERS | `{     'OCU',           # OCU 강의자료 — 원본 보존     '03_OCU',        # OCU 강의자료 (구 폴더명` |
| CLIP_CONFIG_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "clip_config.json")` |
| CLIP_INDEX_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "클리핑_인덱스.json")` |
| CLIP_FOLDER | `os.path.join(OBSIDIAN_VAULT_PATH, "클리핑")` |
| MAX_CLIP_FILE_KB | `200` |
| CLIP_DEDUP_DAYS | `1` |
| CLIP_DEFAULTS | `{     "topics": [         # ── 온유 시스템 개선 참고용 (시스템 관련만) ──         "Anthropic Cla` |
| API_KEY | `os.environ.get("GEMINI_API_KEY")` |
| LOCATION_CONFIG_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "onew_location.json")` |
| SENSITIVE_KEYWORDS | `[     "양악", "수술", "산재", "요양", "우울", "힘들어", "외로워", "불안",     "상처", "아파", "속상", "고` |
| TIME_QUERY_KEYWORDS | `[     "어제", "오늘", "최근", "요즘", "이번 주", "지난주", "이번달", "지난달",     "방금", "아까", "최신",` |
| VAGUE_REFS | `[     "그거", "그것", "그게", "그걸", "그거랑", "그거를",     "아까", "저번에", "그때", "이전에", "방금", ` |
| ENTITY_MEMORY_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "onew_entities.json")` |
| AUTO_SCRIPTS_DIR | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "auto_scripts")` |
| _SHELL_SAFELIST | `[     "pip install", "pip uninstall", "pip list", "pip show", "pip freeze",     ` |
| CALENDAR_CREDS_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "calendar_credentials.json")` |
| CALENDAR_TOKEN_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "calendar_token.json")` |
| CALENDAR_SCOPES | `["https://www.googleapis.com/auth/calendar"]` |
| QUIZ_HISTORY_FILE | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "quiz_history.json")` |
| NIGHT_STUDY_DIR | `os.path.join(OBSIDIAN_VAULT_PATH, "야간학습")` |
| QUIZ_COOLDOWN_DAYS | `3` |
| TTS_MAX_CHARS | `500` |
| VOICE_SAMPLE_RATE | `16000` |
| VOICE_SILENCE_THRESHOLD | `0.008` |
| VOICE_SILENCE_DURATION | `1.5` |
| VOICE_MAX_DURATION | `30` |
| VOICE_PROFILE_PATH | `os.path.join(OBSIDIAN_VAULT_PATH, "SYSTEM", "voice_profile.npy")` |

---

## 2. 클래스 구조

### _MetaCompat (줄 536)

| 줄 | 메서드 | 설명 |
|----|--------|------|
| 537 | __init__() |  |
| 541 | get() |  |
| 546 | setdefault() |  |
| 551 | __len__() |  |
| 559 | __contains__() |  |

### OnewPureMemory (줄 563)

| 줄 | 메서드 | 설명 |
|----|--------|------|
| 564 | __init__() |  |
| 575 | db() | 외부 코드 하위호환: onew.mem.db.get('__meta__', {}).get(key) 패턴 지원 |
| 580 | _init_tables() |  |
| 598 | _load_db() |  |
| 627 | _migrate_from_json() | 기존 onew_pure_db.json → LanceDB 1회 마이그레이션 |
| 664 | _save_meta() |  |
| 676 | _save_db() | LanceDB는 자동 저장. 백업 폴더에 스냅샷만 남김. |
| 699 | _ensure_loaded() |  |
| 705 | _reload_if_updated() | LanceDB는 항상 최신 상태이므로 ensure_loaded만 호출. |
| 710 | sync() |  |
| 951 | embed_single_file() | 단일 파일을 즉시 임베딩하여 LanceDB에 추가/갱신한다. |
| 1004 | search() |  |
| 1116 | recalculate_importance() |  |

### AutoClipper (줄 1845)

| 줄 | 메서드 | 설명 |
|----|--------|------|
| 1846 | __init__() |  |
| 1852 | start() |  |
| 1859 | stop() |  |
| 1863 | _run() |  |
| 2002 | _update_list_md() |  |
| 2025 | status() |  |

### OnewAgent (줄 3345)

| 줄 | 메서드 | 설명 |
|----|--------|------|
| 3346 | __init__() |  |
| 3366 | _save_history_to_vault() |  |
| 3405 | _save_session_summary() | 대화에서 학습한 내용 요약 → 대화요약/ 에 저장 (RAG 인덱싱 대상) |
| 3462 | _build_system_prompt() | 위치 모드에 따라 시스템 프롬프트를 다르게 구성. |
| 3642 | _check_unfinished_tasks() | WORKING_MEMORY_DIR에서 미완료 체크리스트를 감지하여 재개 프롬프트 반환. |
| 3717 | _new_chat_session() | 채팅 세션만 초기화 (메모리 DB는 유지). 이전 맥락 요약 주입 가능. |
| 3807 | set_secret_mode() | 시크릿 모드를 수동으로 설정하고 onew_location.json에 저장. |
| 3825 | clear_secret_override() | 수동 오버라이드를 제거 → 이후 WiFi 자동 감지로 복귀. |
| 3841 | _classify_session() | 대화 내용을 '휘발성' 또는 '장기'로 분류.
- 휘발성: 일상 잡담, 단순 질문, 검색 1회성 응답
- 장기: 학습 내용, 결정 사항, 파일 수정, 태스크, 계획
턴이 4개 미만 |
| 3866 | _summarize_last_session() | 저장된 마지막 대화 파일을 읽어 요약 반환.
휘발성 대화는 요약 생략 → 세션 리셋 시 맥락 주입 안 함 (API 절약). |
| 3894 | _load_recent_summaries() | 최근 N일치 대화요약 파일을 읽어 다일 맥락 문자열 반환 (API 호출 없음). |
| 3921 | _load_entities() | 저장된 엔티티 워킹메모리 로드. |
| 3932 | _save_entities() | 엔티티 워킹메모리 저장. |
| 3942 | _merge_person_entities_to_md() | onew_entities.json의 인물 타입 엔티티를 memory/entities.md 테이블에 중복 없이 병합. |
| 4005 | _extract_entities() | 대화에서 핵심 엔티티 추출 후 워킹메모리에 저장 (3턴마다 실행). |
| 4041 | _update_user_profile_facts() | 대화에서 용준 관련 새 사실 추출 → User_Profile.md '최근 상태' 섹션에 누적.
6턴마다 실행. 건강/목표진척/결정/감정 등 장기적으로 기억할 정보만 추출. |
| 4134 | _reset_chat_if_needed() |  |
| 4167 | ask() |  |

### WakeWordListener (줄 4714)

| 줄 | 메서드 | 설명 |
|----|--------|------|
| 4725 | __init__() |  |
| 4739 | start() |  |
| 4748 | stop() |  |
| 4753 | is_triggered() |  |
| 4759 | _run() |  |

---

## 3. 주요 모듈 레벨 함수

| 줄 | 함수 | 설명 |
|----|------|------|
| 55 | _cleanup_old_logs() |  |
| 86 | _check_single_instance() | 중복 실행 방지 — 이미 실행 중인 온유 프로세스가 있으면 경고 후 종료.
cmdline에 'obsidian_agent'가 있을 때만 차단 (PID 재사용 오감지 방지). |
| 251 | _get_public_ip() | 현재 공인 IP를 반환. 실패 시 빈 문자열. |
| 261 | _get_wifi_security() | 현재 연결된 WiFi 보안 방식을 반환. 'open'(비밀번호없음), 'secured'(비밀번호있음), 'unknown' 중 하나.
PowerShell + UTF-8 강제 출력으로 |
| 307 | detect_location_mode() | WiFi 보안 방식 기반으로 시크릿 모드 결정.
- 개방형 WiFi(비밀번호 없음) → 'work' (시크릿 ON)
- 보안 WiFi(비밀번호 있음)  → 'home' (시크릿 O |
| 350 | cosine_similarity() |  |
| 359 | _save_mistake() | 오답을 두 곳에 저장.
1. 기존 SYSTEM/오답_패턴.md (하위 호환 유지)
2. 온유_성장기록/실패사례/YYYY-MM_실패사례.md (야간학습 대상) |
| 403 | _save_code_lesson() | 에러 반복 감지 시 코드 교훈으로 저장 (야간학습 대상). |
| 425 | _self_evaluate() | 답변 자기 채점. 1~5점 반환. 단순 질문은 5점(스킵)으로 반환. |
| 447 | _semantic_chunks() | 마크다운 헤더(#) 단위로 의미 있게 분할. 섹션이 너무 길면 재분할. |
| 484 | _compress_content() | 대용량 파일을 섹션별로 나눠 Gemini로 압축 요약한다. |
| 506 | _score_importance() | 노트 중요도를 HIGH / MEDIUM / LOW 로 자동 판별. |
| 1154 | read_file() | 지정된 경로의 파일 내용을 읽어옵니다. 코드를 수정하기 전에 먼저 읽어보세요.
파일명만 입력 시(예: '2025-10-17.md') Vault 전체에서 자동으로 파일을 찾습니다. |
| 1174 | create_folder() | Vault 내에 폴더를 생성합니다. 폴더 경로는 절대경로 또는 Vault 기준 상대경로 모두 가능합니다. |
| 1191 | _backup_file() | 수정 전 원본을 code_backup/YYYY-MM-DD/ 에 보존. |
| 1205 | _send_telegram_notify() | 옵시디언 에이전트 → 텔레그램 단방향 알림. |
| 1235 | _review_with_claude_bg() | 백그라운드: Claude API로 코드 리뷰 후 SYSTEM/코드리뷰/에 저장. |
| 1289 | write_file() | 지정된 경로에 파일을 생성하거나 내용을 덮어씁니다. 코드를 작성하거나 수정할 때 사용하세요. |
| 1374 | edit_file() | 파일의 특정 부분만 교체합니다. 전체 재작성 대신 반드시 이 도구를 사용하세요.
old_str: 교체할 기존 텍스트 (파일 내에서 유일해야 함)
new_str: 교체할 새 텍스트 |
| 1453 | list_files() | 지정된 디렉토리 내의 파일 및 폴더 목록을 반환합니다. 구조를 파악할 때 사용하세요.
경로는 절대경로 또는 Vault 기준 상대경로 모두 가능합니다. 경로 생략 시 Vault 루트 |
| 1470 | move_file() | 파일을 src에서 dst로 이동합니다. 바탕화면 → Vault 정리 등에 사용하세요. |
| 1515 | delete_file() | 지정된 파일을 삭제합니다. 파일 이동 후 원본 제거, 불필요한 파일 정리에 사용하세요. |
| 1567 | execute_script() | 작성된 파이썬 스크립트를 즉시 실행하고 결과를 반환한다.
⚠️ 온유가 생성한 자동화 스크립트는 반드시 SYSTEM/auto_scripts/ 폴더에 저장 후 이 도구로 실행하세요.
 |
| 1626 | run_shell_command() | 안전한 쉘 명령어를 실행한다. 주로 패키지 설치 확인, python 모듈 실행에 사용.
허용: pip install/uninstall/list/show/freeze, python  |
| 1652 | browse_web() | Playwright 헤드리스 브라우저로 URL을 열어 마크다운 본문을 반환한다.
JavaScript 렌더링이 필요한 페이지(SPA, 동적 사이트)도 처리 가능.
Jina/urlli |
| 1701 | rollback_file() | 파일을 가장 최근 백업본으로 복원합니다. 코드 수정 후 문제가 생겼을 때 사용하세요. |
| 1732 | backup_system() | SYSTEM 폴더 전체를 ZIP으로 압축 백업한다. |
| 1744 | code_safety_check() | 핵심 코드 파일의 MD5 체크섬을 검증하고 안전 점검 보고서를 반환한다. |
| 1752 | save_code_checksums() | 현재 핵심 코드 파일들의 MD5 체크섬을 베이스라인으로 저장한다. |
| 1760 | _get_search_count() | 오늘의 웹 검색 횟수를 반환한다. |
| 1770 | _get_today_usage() | 오늘 특정 카테고리의 API 호출 횟수를 반환한다. |
| 1780 | _increment_usage() | 카테고리별 API 호출 횟수를 기록한다. category: 'chat', 'rag', 'summary', 'flowchart' |
| 1797 | _load_clip_config() |  |
| 1806 | _save_clip_config() |  |
| 1811 | _load_clip_index() |  |
| 1819 | _save_clip_index() |  |
| 1825 | _is_recently_clipped() |  |
| 1837 | _today_clip_count() |  |
| 2045 | _clip_command() | 자연어 클리핑 명령 처리. 처리됐으면 True 반환. |
| 2122 | clip_status() | 자동 클리핑 현황, 주제 목록, 설정을 보여준다. |
| 2127 | get_clip_topics() | 현재 클리핑 주제 목록을 번호와 함께 반환한다. 주제 추가/삭제 전 반드시 호출하라. |
| 2136 | set_clip_config() | 자동 클리핑 설정을 변경한다.
topics: 새 주제 리스트 (예: ["AI 뉴스", "Python 팁"])
delay_seconds: 클리핑 간격 (초)
max_clips: 하루 |
| 2158 | set_weekly_clip() | 특정 주제를 주간(7일) 클리핑 추적 목록에 추가/제거한다.
topics: 주제 리스트
action: 'add'(추가) 또는 'remove'(제거)
주간 주제는 일반 클리핑 주제와 |
| 2185 | analyze_trend() | Google 검색으로 최신 금융/기술/학습 트렌드를 분석한다. |
| 2220 | _get_calendar_service() | Google Calendar 서비스 객체를 반환한다. 최초 1회 브라우저 인증 필요. |
| 2244 | calendar_list() | 오늘부터 N일간의 구글 캘린더 일정을 조회한다. (기본 7일) |
| 2269 | _normalize_dt() | 날짜시간 문자열을 'YYYY-MM-DDTHH:MM:SS' 형식으로 정규화. |
| 2280 | calendar_add() | 구글 캘린더에 일정을 추가한다. 추가 전 같은 시간대 중복 일정을 자동 확인한다.
start/end 형식: 'YYYY-MM-DDTHH:MM' (예: '2026-03-20T09:00 |
| 2321 | calendar_add_force() | 중복 확인 없이 구글 캘린더에 일정을 강제 추가한다. 중복 경고 후 사용자가 명시적으로 요청한 경우에만 사용. |
| 2336 | calendar_update() | 기존 일정을 수정한다. event_id는 calendar_list로 확인. 변경할 항목만 입력. |
| 2350 | calendar_delete() | 구글 캘린더 일정을 삭제한다. event_id는 calendar_list로 확인. |
| 2359 | check_errors() | 온유_오류.md에서 최근 오류를 읽어 분석하고 원인과 조치를 제안한다. |
| 2386 | log_error_to_vault() | 오류를 Obsidian SYSTEM/온유_오류.md에 날짜별로 누적 기록. 최대 50개 유지. |
| 2406 | _notify_safety_block() | Gemini 응답에서 방어기제(Safety) 차단 여부를 감지하고 텔레그램으로 알린다.
차단이 감지되면 True 반환, 아니면 False 반환.
차단 시 온유_오류.md 최근 5개 |
| 2477 | report_status() | 오늘의 API 사용량, 비용, 저장된 지식 수를 보고한다. |
| 2502 | run_apm_report() | 온유 시스템 APM 일일 리포트를 실행한다.
query_log.jsonl을 분석해 응답시간·캐시 재사용률·프로필 크기·병목 진단 결과를 반환한다.
date: 'YYYY-MM-DD' |
| 2521 | search_vault() | 옵시디언 Vault(개인 노트/일기/공부자료)에서 키워드를 검색한다.
인물, 날짜, 대화 내용, 공부 기록 등 개인 기록을 찾을 때 사용하라.
웹 검색(analyze_trend)과 |
| 2552 | set_secret_mode() | 시크릿 모드를 켜거나 끈다. on=True면 민감 주제 차단, on=False면 해제. |
| 2559 | get_secret_mode() | 현재 시크릿 모드 상태와 설정 방식을 확인한다. (WiFi 자동감지 or 수동 오버라이드) |
| 2588 | fetch_url_as_md() | URL의 웹 페이지 내용을 가져와 마크다운으로 변환하고 클리핑/ 폴더에 저장한다.
- Anthropic 공홈(docs.anthropic.com, anthropic.com) → 클리 |
| 2694 | analyze_image() | 이미지 파일을 Gemini Vision으로 분석한다. question이 없으면 전체 내용을 설명한다. |
| 2738 | _load_quiz_history() | 최근 출제된 문제 해시 목록 로드. |
| 2748 | _save_quiz_history() |  |
| 2754 | _clean_latex() | LaTeX 수식을 읽기 쉬운 텍스트로 변환. |
| 2821 | _is_calc_heavy() | 계산기가 필요한 수치 계산 문제 여부 판별. |
| 2841 | _question_hash() |  |
| 2845 | _parse_questions_from_file() | 야간학습 .md 파일에서 문제/정답/해설 파싱.
형식:
  1.  (문제) 실제 문제 텍스트
      정답: 정답 텍스트 (멀티라인 가능)
      해설: 해설 텍스트 |
| 2909 | quiz_me() | 야간학습에서 예상 문제를 가져와 퀴즈 세트를 반환합니다.
category: '소방' / 'OCU' / 'AI클리핑' / '현장' / None(전체)
count: 출제 문제 수 (기 |
| 3144 | load_skills_metadata() | SYSTEM/skills/ 폴더의 스킬 파일들에서 name과 description만 추출해 반환. |
| 3171 | create_working_memory() | 복잡한 작업 시작 전 Plan.md, Context.md, Checklist.md 3개 파일을 working_memory 폴더에 생성합니다.

Args:
    task_name: |
| 3268 | refresh_code_index() | obsidian_agent.py 코드 구조를 AST로 파싱해 온유_코드구조.md를 재생성합니다.
Gemini API 호출 없음. 토큰 소모 없음. Claude Code가 코드를 수 |
| 4562 | speak() | 텍스트를 edge-tts(Microsoft 한국어 여성 신경망 음성)로 출력. |
| 4612 | _get_whisper_model() |  |
| 4621 | _record_until_silence() | 마이크 입력 녹음. 침묵 감지 시 자동 종료. |
| 4655 | voice_input() | 음성 녹음 → 텍스트 변환. 실패 시 빈 문자열 반환. |
| 4679 | _extract_mfcc() | librosa로 MFCC 40차 추출 → 평균 벡터 반환. |
| 4685 | _cosine_similarity() |  |
| 4690 | enroll_voice() | 용준님 목소리 등록 (10초 녹음 → MFCC 성문 저장). |
| 4703 | verify_voice() | 녹음된 음성이 등록된 성문과 유사한지 확인. |
| 4885 | _auto_detect_claude_work() | 새 Claude Code 작업일지 감지 → 보완사항 자동 생성.
개별 파일 대신 onew_work_index.md(압축 요약본)를 사용 — 파일 수 제한 없음. |
| 4966 | _startup_briefing() | 시작 시 학습 진도 알림 + 오늘 클리핑 뉴스 브리핑을 음성으로 출력. |
| 5004 | _send_telegram_notify() | 봇으로 용준님에게 텔레그램 알림 전송 |
| 5026 | _auto_backup() | 시작 시 SYSTEM 코드 파일(.py, .md)을 code_backup/YYYY-MM-DD/ 에 자동 백업. |
| 5046 | _print_error_summary() | 종료 시 오늘 발생한 에러 요약 출력. |

---

## 4. 온유 도구 함수 (onew_tools 등록됨)

| 함수명 |
|--------|
| read_file |
| write_file |
| edit_file |
| create_folder |
| list_files |
| move_file |
| delete_file |
| execute_script |
| run_shell_command |
| browse_web |
| rollback_file |
| backup_system |
| code_safety_check |
| save_code_checksums |
| analyze_trend |
| calendar_list |
| calendar_add |
| calendar_add_force |
| calendar_update |
| calendar_delete |
| check_errors |
| report_status |
| get_secret_mode |
| set_secret_mode |
| run_apm_report |
| search_vault |
| clip_status |
| get_clip_topics |
| set_clip_config |
| set_weekly_clip |
| fetch_url_as_md |
| analyze_image |
| quiz_me |
| create_working_memory |
| refresh_code_index |

---

> 원본: `SYSTEM/obsidian_agent.py` (277KB, 5782줄)
> 마지막 갱신: 2026-03-29 18:19